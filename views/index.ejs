<html>
    <title><%= title %></title>
    <body>
        <form action="/new"><button>New Message</button></form>
        <% messages.forEach((item) => { %>
            <p><%= item.username %>: <%= item.message %></p>
            <p>- <%= item.added %></p>
        <% }); %>
    </body>
</html>